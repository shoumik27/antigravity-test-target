## Setup Verification

After cloning, developers must verify their environment is correctly
configured by running the startup check defined in src/main.py comments.
This is enforced by the CI pipeline.